# Core Deep Learning Dependencies
# PyTorch with CUDA 12.9 (install separately with specific index-url)
# torch>=2.8.0
# torchvision
# torchaudio
pytorch-lightning>=2.0.0

# FlashAttention 2 (requires CUDA toolkit, install with --no-build-isolation)
# flash-attn

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# ML Utilities
scikit-learn>=1.3.0

# Optional: Experiment Tracking
wandb>=0.16.0

# Optional: Optimized Optimizers
# nvidia-apex (install from source for FusedAdam optimizer)

# Development Tools
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

rich>=14.0.0
tensorboardX>=2.6.4
peewee >= 3.18.2
torch_optimizer>=0.3.0
json5>=0.12.1
